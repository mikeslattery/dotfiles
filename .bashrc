# .bashrc

# .zshrc works nominally as a .bashrc
# I don't try to make bash work as well, just enough to not be broken.
# Other bash files should NOT be in git (e.g. ~/.profile)

source "$HOME/.zshrc"

